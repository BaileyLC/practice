## Schaeffer Lab Practice

# Prepare workspace ----

# Set working directory
  setwd("~/Downloads")

# Create a data frame
  df <- data.frame("A" = c(1:10),
                   "B" = c(11:20))
  
# new col - Daniel
    df$D <- c(47:1200)